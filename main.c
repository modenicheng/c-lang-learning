#include <stdio.h>
#include <math.h>

int main()
{
    // double a, b, c, d, x;
    // int r = scanf("(%lf,%lf) (%lf,%lf)", &a, &b, &c, &d);
    // printf("r = %d\n", r);
    // x = sqrt((a - c) * (a - c) + (b - d) * (b - d));
    // printf("%.2lf\n", x);
    // return 0;

    // char c = 'A';
    // int i = 1, j;

    // j = !c && i++;

    // printf("%d %d", i, j);

    int a = 125;
    printf("%d,%o,%x\n", a, a + 1, a + 2);
}